# Агрегатор календарей — Полное руководство

**Дата создания:** 12 ноября 2025 г.  
**Версия:** 1.0  
**Статус:** Актуально для OmniCal 3.5+

---

## 📋 СОДЕРЖАНИЕ

1. [Что такое агрегатор календарей](#что-такое-агрегатор-календарей)
2. [Как работает текущий агрегатор Calendar 5](#как-работает-текущий-агрегатор-calendar-5)
3. [Архитектура агрегатора](#архитектура-агрегатора)
4. [Поток данных в агрегаторе](#поток-данных-в-агрегаторе)
5. [Как создать новый агрегатор с нуля](#как-создать-новый-агрегатор-с-нуля)
6. [Расширение текущего агрегатора](#расширение-текущего-агрегатора)
7. [Отладка и troubleshooting](#отладка-и-troubleshooting)
8. [FAQ](#faq)

---

## 🎯 ЧТО ТАКОЕ АГРЕГАТОР КАЛЕНДАРЕЙ

### Концепция (Concept)

**Агрегатор** — это специальный календарь, который объединяет несколько источников (подкалендарей) и отображает их события с **единым оформлением**.

### Зачем это нужно? (Why is this needed?)

**Проблема:**
- У пользователя есть 5 календарей: Личный, Работа, Проекты, Хобби, Семья
- Rainmeter скин поддерживает только 5 календарей (Cal_1 - Cal_5)
- Каждый календарь имеет свой цвет → 5 разных цветов на экране

**Решение с агрегатором:**
- Cal_1 = Календарь 1 (синий)
- Cal_2 = Календарь 2 (зелёный)
- Cal_3 = Календарь 3 (красный)
- Cal_4 = Календарь 4 (жёлтый)
- **Cal_5 = АГРЕГАТОР** (фиолетовый) = Личный + Работа + Проекты + Хобби + Семья

Теперь можно отображать **9 календарей вместо 5**, и все события агрегатора будут одного цвета (фиолетовые).

### Ключевые особенности (Key features)

✅ **Единое оформление**: Все события агрегатора имеют один `SourceFeed` → один цвет  
✅ **Гибкость**: Можно добавить/удалить подкалендари, не меняя код  
✅ **Прозрачность**: Пустые URL автоматически пропускаются  
✅ **Сортировка**: События из разных подкалендарей корректно упорядочиваются  
✅ **Управление**: Включение/выключение всего агрегатора одной кнопкой

---

## 🔍 КАК РАБОТАЕТ ТЕКУЩИЙ АГРЕГАТОР CALENDAR 5

### Структура (Structure)

```
Calendar 5 (Агрегатор)
├── iCalendar5_1 → MeasureFeed5_1 → События с SourceFeed=5
├── iCalendar5_2 → MeasureFeed5_2 → События с SourceFeed=5
├── iCalendar5_3 → MeasureFeed5_3 → События с SourceFeed=5
├── iCalendar5_4 → MeasureFeed5_4 → События с SourceFeed=5
└── iCalendar5_5 → MeasureFeed5_5 → События с SourceFeed=5

Все события → цвет Cal_5_Bg
```

### Переменные в Variables_URL.ini

```ini
; Агрегатор Calendar 5 (Aggregator Calendar 5)
iCalendar5_name = EQ-Таюшка (Агрегатор)

; Подкалендари агрегатора (Subcalendars of aggregator)
iCalendar5_1=https://p141-caldav.icloud.com/.../Личный
iCalendar5_2=https://p141-caldav.icloud.com/.../Консультации
iCalendar5_3=https://p141-caldav.icloud.com/.../Проекты
iCalendar5_4=  ; Пусто (Empty)
iCalendar5_5=  ; Пусто (Empty)
```

**Важно:**
- Переменная `iCalendar5` **НЕ существует** (заменена на подкалендари)
- Переменная `iCalendar5_name` — общее название агрегатора
- Пустые URL (`iCalendar5_4`, `iCalendar5_5`) просто игнорируются

### Меры WebParser в OmniCal.ini

```ini
[MeasureFeed5_1]
Measure=WebParser
URL=#iCalendar5_1#
RegExp=(?siU)(.*)$
FinishAction=[!CommandMeasure Reader_lua Refresh(1)]

[MeasureFeed5_2]
Measure=WebParser
URL=#iCalendar5_2#
...

[MeasureFeed5_3]
[MeasureFeed5_4]
[MeasureFeed5_5]
```

**Важно:**
- Старая мера `[MeasureFeed5]` **удалена**
- Создано 5 независимых мер `[MeasureFeed5_1]` - `[MeasureFeed5_5]`
- Каждая мера парсит свой URL (`#iCalendar5_N#`)

### ActiveFeeds (логическое представление)

```ini
; В Variables_URL.ini:
ActiveFeeds=MeasureFeed1|MeasureFeed2|MeasureFeed5
```

**Обратите внимание:**
- В `ActiveFeeds` содержится **MeasureFeed5** (без суффикса `_1`, `_2` и т.д.)
- Это **логический идентификатор** агрегатора
- **Физические меры**: `MeasureFeed5_1`, `MeasureFeed5_2`, ..., `MeasureFeed5_5`

### Логика в Reader.lua

#### 1. Initialize() и Update() — Автопоиск подкалендарей

```lua
for MeasureName in AllMeasureNames:gmatch('[^%|]+') do
    if MeasureName == 'MeasureFeed5' then
        -- Специальная обработка для агрегатора (Special handling for aggregator)
        for subIdx = 1, 5 do
            local subMeasureName = 'MeasureFeed5_' .. subIdx
            local subMeasure = SKIN:GetMeasure(subMeasureName)
            if subMeasure then
                local url = SKIN:GetVariable('iCalendar5_' .. subIdx)
                if url and url:match('https?://') then
                    table.insert(Feeds, {
                        Measure = subMeasure,
                        MeasureName = subMeasureName,
                        ...
                    })
                end
            end
        end
    else
        -- Обычная обработка для MeasureFeed1-4 (Normal processing)
        table.insert(Feeds, {...})
    end
end
```

**Что происходит:**
1. Reader.lua получает `ActiveFeeds = "MeasureFeed1|MeasureFeed5"`
2. При встрече `MeasureFeed5` запускается специальная логика
3. Поиск физических мер `MeasureFeed5_1` - `MeasureFeed5_5`
4. Проверка URL: если `iCalendar5_N` не пустой → добавить в `Feeds[]`
5. Пустые URL пропускаются

**Результат:**
```lua
Feeds = {
    {MeasureName = "MeasureFeed1", ...},   -- Обычный календарь
    {MeasureName = "MeasureFeed5_1", ...}, -- Подкалендарь 1
    {MeasureName = "MeasureFeed5_2", ...}, -- Подкалендарь 2
    {MeasureName = "MeasureFeed5_3", ...}, -- Подкалендарь 3
    -- MeasureFeed5_4 и 5_5 пропущены (пустые URL)
}
```

#### 2. Input() — Извлечение SourceFeed

```lua
local sourceNum = tonumber(Feeds[f].MeasureName:match('MeasureFeed(%d+)')) or f
Item.SourceFeed = sourceNum
Item.MeasureName = Feeds[f].MeasureName
```

**Regex:** `MeasureFeed(%d+)` — берёт **первое** число после `MeasureFeed`

**Примеры:**
- `MeasureFeed1` → `sourceNum = 1`
- `MeasureFeed5_1` → `sourceNum = 5` (берём первое число!)
- `MeasureFeed5_2` → `sourceNum = 5`
- `MeasureFeed5_3` → `sourceNum = 5`

**Результат:** Все события из подкалендарей получают `SourceFeed = 5` → единый цвет `Cal_5_Bg`

#### 3. compareTime() — Сортировка событий

```lua
function compareTime(a, b)
    -- Сортировка по времени (Sort by time)
    if a.Date ~= b.Date then
        return a.Date < b.Date
    end
    
    -- Если время одинаковое, сортировка по номеру подкалендаря
    -- (If time is the same, sort by subcalendar number)
    local aSubIdx = tonumber((a.MeasureName or ''):match('_(%d+)$')) or 0
    local bSubIdx = tonumber((b.MeasureName or ''):match('_(%d+)$')) or 0
    
    return aSubIdx < bSubIdx
end
```

**Regex:** `_(%d+)$` — последнее число после `_`

**Примеры:**
- `MeasureFeed5_1` → `subIdx = 1`
- `MeasureFeed5_3` → `subIdx = 3`
- `MeasureFeed1` → `subIdx = 0` (нет суффикса)

**Логика:**
1. Сначала сортировка по времени (раньше → левее)
2. Если время одинаковое → по порядку подкалендаря (5_1 левее 5_3)

**Пример:**
```
14:00 - Встреча (5_1)    ← левее
14:00 - Звонок (5_3)     ← правее
15:00 - Обед (5_2)
```

### Логика в OmniCal_Skin.lua — ToggleFeed()

#### 1. Определение validFeeds

```lua
for n = 1, 10 do
    if n == 5 then
        -- Специальная обработка для агрегатора (Special handling for aggregator)
        local hasValidSubcalendar = false
        for subIdx = 1, 5 do
            local urlSub = SKIN:GetVariable('iCalendar5_' .. subIdx)
            if urlSub and urlSub:match('https?://') then
                hasValidSubcalendar = true
                break
            end
        end
        if hasValidSubcalendar then
            table.insert(validFeeds, 5)
        end
    else
        -- Обычная проверка для календарей 1-4 (Normal check)
        local url = SKIN:GetVariable('iCalendar' .. n)
        if url and url:match('https?://') then
            table.insert(validFeeds, n)
        end
    end
end
```

**Что происходит:**
- Для n=1-4: проверяем `iCalendar1`, `iCalendar2`, и т.д.
- Для n=5: проверяем **хотя бы один** из `iCalendar5_1` - `iCalendar5_5`
- Если найден хотя бы один непустой URL → календарь 5 валиден

#### 2. Управление URL в мерах

```lua
for i = 1, 10 do
    if i == 5 then
        -- Управлять всеми подкалендарями (Manage all subcalendars)
        -- Пустая строка для визуального разграничения в логах (Empty line for visual separation in logs)
        if tonumber(SKIN:GetVariable('DebugMode')) == 1 then
            print("")
        end
        
        for subIdx = 1, 5 do
            local urlSubVar = 'iCalendar5_' .. subIdx
            local urlSub = SKIN:GetVariable(urlSubVar)
            local measureSubName = 'MeasureFeed5_' .. subIdx
            
            if present[i] then
                -- Включить (Enable)
                if urlSub and urlSub:match('https?://') then
                    if tonumber(SKIN:GetVariable('DebugMode')) == 1 then
                        print("~~~~~~~~TF.ON: " .. measureSubName .. " " .. urlSubVar .. " " .. shortenUrl(urlSub))
                    end
                    SKIN:Bang('!SetOption', measureSubName, 'URL', urlSub)
                else
                    SKIN:Bang('!SetOption', measureSubName, 'URL', '')
                end
            else
                -- Отключить (Disable)
                if tonumber(SKIN:GetVariable('DebugMode')) == 1 then
                    print("~~~~~~~~TF.OFF: " .. measureSubName .. " " .. urlSubVar .. " ''")
                end
                SKIN:Bang('!SetOption', measureSubName, 'URL', '')
            end
            SKIN:Bang('!UpdateMeasure', measureSubName)
        end
    else
        -- Обычная обработка для календарей 1-4 (Normal processing)
        ...
    end
end
```

**Что происходит:**
- **Пустой `print("")`** перед циклом агрегатора создаёт визуальное разделение в логах
- При включении Cal_5: логируется и устанавливается URL для всех 5 подмер (кроме пустых)
- При отключении Cal_5: логируется и очищается URL для всех 5 подмер
- Пустой URL = мера не парсит данные (стандартное поведение WebParser)
- Функция `shortenUrl()` сокращает длинные URL в логах (макс. 50 символов)

**Пример логов с разграничением:**
```
DBUG: ===TF: ON  MeasureFeed1 | iCalendar1 = https://...
DBUG: ===TF: ON  MeasureFeed2 | iCalendar2 = https://...
DBUG:                                              ← ПУСТАЯ СТРОКА
DBUG: ~~~~~~~~TF.ON: MeasureFeed5_1 iCalendar5_1 https://p141-caldav...
DBUG: ~~~~~~~~TF.ON: MeasureFeed5_2 iCalendar5_2 https://p141-caldav...
DBUG: ~~~~~~~~TF.ON: MeasureFeed5_3 iCalendar5_3 https://p19-caldav...
```

---

## 🏗️ АРХИТЕКТУРА АГРЕГАТОРА

### Компоненты (Components)

```
┌─────────────────────────────────────────────────────────────┐
│                    АГРЕГАТОР CALENDAR 5                      │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
┌───────────────┐   ┌───────────────┐   ┌───────────────┐
│Variables_URL  │   │  OmniCal.ini  │   │ OmniCal_Skin  │
│     .ini      │   │               │   │     .lua      │
├───────────────┤   ├───────────────┤   ├───────────────┤
│iCalendar5_1   │──▶│MeasureFeed5_1 │   │ToggleFeed(5)  │
│iCalendar5_2   │──▶│MeasureFeed5_2 │   │ - validFeeds  │
│iCalendar5_3   │──▶│MeasureFeed5_3 │   │ - URL control │
│iCalendar5_4   │──▶│MeasureFeed5_4 │   │               │
│iCalendar5_5   │──▶│MeasureFeed5_5 │   │               │
└───────────────┘   └───────┬───────┘   └───────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │  Reader.lua   │
                    ├───────────────┤
                    │ Initialize()  │ ← Автопоиск подмер
                    │ Update()      │ ← Перестроение Feeds[]
                    │ Input()       │ ← Извлечение SourceFeed=5
                    │ compareTime() │ ← Сортировка
                    └───────┬───────┘
                            │
                            ▼
                    ┌───────────────┐
                    │ OmniCal_Events│
                    │     .lua      │
                    ├───────────────┤
                    │Классификация  │ ← Группы событий
                    │по времени     │
                    └───────┬───────┘
                            │
                            ▼
                    ┌───────────────┐
                    │OmniCal_Events │
                    │_Renderer.lua  │
                    ├───────────────┤
                    │Генерация Shape│ ← Цвет Cal_5_Bg
                    │метров         │
                    └───────────────┘
```

### Ключевые принципы (Key principles)

1. **Разделение ответственности (Separation of concerns)**:
   - Variables_URL.ini → хранение URL
   - OmniCal.ini → парсинг .ics файлов
   - Reader.lua → преобразование в события
   - OmniCal_Skin.lua → управление включением/выключением

2. **Логический vs Физический (Logical vs Physical)**:
   - **Логический**: `MeasureFeed5` (в ActiveFeeds)
   - **Физический**: `MeasureFeed5_1`, `MeasureFeed5_2`, ..., `MeasureFeed5_5` (реальные меры)

3. **Автоматическое связывание (Automatic binding)**:
   - Reader.lua при встрече `MeasureFeed5` автоматически ищет физические меры
   - Не нужно вручную прописывать все 5 мер в ActiveFeeds

4. **Единое оформление (Unified styling)**:
   - Все события получают `SourceFeed = 5`
   - Цвет определяется переменной `Cal_5_Bg`

---

## 📊 ПОТОК ДАННЫХ В АГРЕГАТОРЕ

### Шаг 1: Пользователь включает Cal_5

```
UI: Клик на кнопку Cal_5
   ↓
OmniCal_Skin.lua: ToggleFeed(5)
   ↓
Проверка validFeeds: есть ли непустые iCalendar5_1-5?
   ↓ ДА
Добавить 'MeasureFeed5' в ActiveFeeds
   ↓
Записать в Variables_URL.ini: ActiveFeeds=...MeasureFeed5
   ↓
Для каждого subIdx = 1-5:
   - Если iCalendar5_N не пустой → установить URL в MeasureFeed5_N
   - Иначе → установить пустой URL
   ↓
Обновить меры: !UpdateMeasure MeasureFeed5_1 ... MeasureFeed5_5
```

### Шаг 2: WebParser парсит .ics файлы

```
MeasureFeed5_1: URL=https://... → Скачать .ics → FinishAction
MeasureFeed5_2: URL=https://... → Скачать .ics → FinishAction
MeasureFeed5_3: URL=https://... → Скачать .ics → FinishAction
MeasureFeed5_4: URL='' → Ничего не делать
MeasureFeed5_5: URL='' → Ничего не делать
   ↓
FinishAction: [!CommandMeasure Reader_lua Refresh(1)]
```

### Шаг 3: Reader.lua обрабатывает события

```
Reader.lua: Refresh(1)
   ↓
Update(): Проверка ActiveFeeds → содержит 'MeasureFeed5'
   ↓
Перестроение Feeds[]:
   - Встречаем 'MeasureFeed5'
   - Запускаем автопоиск:
     for subIdx = 1, 5 do
       Проверяем iCalendar5_N → если не пустой → добавить в Feeds[]
     end
   ↓
Feeds[] = {
    {MeasureName = "MeasureFeed5_1", ...},
    {MeasureName = "MeasureFeed5_2", ...},
    {MeasureName = "MeasureFeed5_3", ...}
}
   ↓
Input(f): Для каждого Feeds[f]
   - Парсинг .ics файла
   - Извлечение событий
   - sourceNum = MeasureFeed5_1 → 5
   - Item.SourceFeed = 5
   - Item.MeasureName = "MeasureFeed5_1"
   ↓
Merge(): Объединение событий из всех подкалендарей
   ↓
compareTime(): Сортировка по времени + по subIdx
   ↓
Items[] = [Event1(5_1), Event2(5_2), Event3(5_1), ...]
```

### Шаг 4: OmniCal_Events.lua классифицирует

```
OmniCal_Events.lua: MeterGroup()
   ↓
Получить Items[] из Reader.lua
   ↓
Для каждого события:
   - SourceFeed = 5 → цвет Cal_5_Bg
   - Классификация по группам (Completed, Current, Future)
   ↓
Генерация переменных: Item1Title, Item1SourceFeed=5, ...
```

### Шаг 5: Отображение на экране

```
OmniCal_Events_Renderer.lua
   ↓
Генерация Shape метров:
   - Shape=Rectangle ... | Fill Color Cal_5_Bg
   ↓
UI: События отображаются с единым цветом (Cal_5_Bg)
```

---

## 🛠️ КАК СОЗДАТЬ НОВЫЙ АГРЕГАТОР С НУЛЯ

### Пример: Создание агрегатора Calendar 6

Допустим, вы хотите создать **второй агрегатор** для Calendar 6 с 3 подкалендарями.

---

#### ШАГ 1: Variables_URL.ini

Добавьте переменные для агрегатора:

```ini
; Агрегатор Calendar 6 (Aggregator Calendar 6)
iCalendar6_name = Мои проекты (Агрегатор)

; Подкалендари агрегатора (Subcalendars of aggregator)
iCalendar6_1=https://calendar.google.com/.../Проект-А
iCalendar6_2=https://calendar.google.com/.../Проект-Б
iCalendar6_3=https://calendar.google.com/.../Проект-В
```

**Важно:** Удалите переменную `iCalendar6` (если существует).

---

#### ШАГ 2: OmniCal.ini

Создайте WebParser меры для подкалендарей:

```ini
;═══════════════════════════════════════════════════════════════════
; Агрегатор iCalendar6: 3 подкалендаря (Aggregator iCalendar6: 3 subcalendars)
;═══════════════════════════════════════════════════════════════════

[MeasureFeed6_1]
Measure=WebParser
UpdateRate=60
URL=#iCalendar6_1#
RegExp=(?siU)(.*)$
DecodeCharacterReference=1
Substitute=""
FinishAction=[!CommandMeasure Reader_lua Refresh(1)][!UpdateMeasure OmniCal_Skin_lua]
Disabled=0
DynamicVariables=1

[MeasureFeed6_2]
Measure=WebParser
UpdateRate=60
URL=#iCalendar6_2#
RegExp=(?siU)(.*)$
DecodeCharacterReference=1
Substitute=""
FinishAction=[!CommandMeasure Reader_lua Refresh(1)][!UpdateMeasure OmniCal_Skin_lua]
Disabled=0
DynamicVariables=1

[MeasureFeed6_3]
Measure=WebParser
UpdateRate=60
URL=#iCalendar6_3#
RegExp=(?siU)(.*)$
DecodeCharacterReference=1
Substitute=""
FinishAction=[!CommandMeasure Reader_lua Refresh(1)][!UpdateMeasure OmniCal_Skin_lua]
Disabled=0
DynamicVariables=1
```

**Важно:** Удалите старую меру `[MeasureFeed6]` (если существует).

---

#### ШАГ 3: Reader.lua — Initialize()

Добавьте обработку для `MeasureFeed6`:

```lua
for MeasureName in AllMeasureNames:gmatch('[^%|]+') do
    -- Специальная обработка для агрегатора MeasureFeed5
    if MeasureName == 'MeasureFeed5' then
        for subIdx = 1, 5 do
            local subMeasureName = 'MeasureFeed5_' .. subIdx
            local subMeasure = SKIN:GetMeasure(subMeasureName)
            if subMeasure then
                local urlVar = 'iCalendar5_' .. subIdx
                local url = SKIN:GetVariable(urlVar)
                if url and url:match('https?://') then
                    table.insert(Feeds, {
                        Measure = subMeasure,
                        MeasureName = subMeasureName,
                        Raw = nil, Type = nil, Title = nil, Link = nil, Error = nil
                    })
                end
            end
        end
    -- Специальная обработка для агрегатора MeasureFeed6 (NEW!)
    elseif MeasureName == 'MeasureFeed6' then
        for subIdx = 1, 3 do  -- ← 3 подкалендаря
            local subMeasureName = 'MeasureFeed6_' .. subIdx
            local subMeasure = SKIN:GetMeasure(subMeasureName)
            if subMeasure then
                local urlVar = 'iCalendar6_' .. subIdx
                local url = SKIN:GetVariable(urlVar)
                if url and url:match('https?://') then
                    table.insert(Feeds, {
                        Measure = subMeasure,
                        MeasureName = subMeasureName,
                        Raw = nil, Type = nil, Title = nil, Link = nil, Error = nil
                    })
                end
            end
        end
    else
        -- Обычная обработка для MeasureFeed1-4
        table.insert(Feeds, {...})
    end
end
```

**Важно:** Повторите аналогичную логику в функции `Update()`.

---

#### ШАГ 4: OmniCal_Skin.lua — ToggleFeed()

##### 4.1. Определение validFeeds

```lua
for n = 1, 10 do
    if n == 5 then
        -- Агрегатор MeasureFeed5
        local hasValidSubcalendar = false
        for subIdx = 1, 5 do
            local urlSub = SKIN:GetVariable('iCalendar5_' .. subIdx)
            if urlSub and urlSub:match('https?://') then
                hasValidSubcalendar = true
                break
            end
        end
        if hasValidSubcalendar then
            table.insert(validFeeds, 5)
        end
    elseif n == 6 then
        -- Агрегатор MeasureFeed6 (NEW!)
        local hasValidSubcalendar = false
        for subIdx = 1, 3 do  -- ← 3 подкалендаря
            local urlSub = SKIN:GetVariable('iCalendar6_' .. subIdx)
            if urlSub and urlSub:match('https?://') then
                hasValidSubcalendar = true
                break
            end
        end
        if hasValidSubcalendar then
            table.insert(validFeeds, 6)
        end
    else
        -- Обычная проверка для календарей 1-4
        local url = SKIN:GetVariable('iCalendar' .. n)
        if url == nil then break end
        if url:match('https?://') then
            table.insert(validFeeds, n)
        end
    end
end
```

##### 4.2. Управление URL в мерах

```lua
for i = 1, 10 do
    if i == 5 then
        -- Агрегатор MeasureFeed5
        for subIdx = 1, 5 do
            -- ... (существующий код)
        end
    elseif i == 6 then
        -- Агрегатор MeasureFeed6 (NEW!)
        for subIdx = 1, 3 do  -- ← 3 подкалендаря
            local urlSubVar = 'iCalendar6_' .. subIdx
            local urlSub = SKIN:GetVariable(urlSubVar)
            local measureSubName = 'MeasureFeed6_' .. subIdx
            
            if present[i] then
                -- Включить
                if urlSub and urlSub:match('https?://') then
                    SKIN:Bang('!SetOption', measureSubName, 'URL', urlSub)
                else
                    SKIN:Bang('!SetOption', measureSubName, 'URL', '')
                end
            else
                -- Отключить
                SKIN:Bang('!SetOption', measureSubName, 'URL', '')
            end
            SKIN:Bang('!UpdateMeasure', measureSubName)
        end
    else
        -- Обычная обработка для календарей 1-4
        -- ... (существующий код)
    end
end
```

---

#### ШАГ 5: OmniCal_Skin.lua — Добавьте функцию ToggleMeasureFeed6()

**Файл:** `@Resources/OmniCal_Skin.lua`

**Что делаем:**
Создать функцию-обёртку для вызова ToggleFeed(6) из кнопки интерфейса.

**Где добавить:**
В конце файла OmniCal_Skin.lua, после существующих функций ToggleMeasureFeed1-5.

**Найти существующие функции (строка ~3540):**
```lua
-- Функция для переключения MeasureFeed1 (Toggle MeasureFeed1)
function ToggleMeasureFeed1()
    ToggleFeed(1)
end

-- Функция для переключения MeasureFeed2 (Toggle MeasureFeed2)
function ToggleMeasureFeed2()
    ToggleFeed(2)
end

-- Функция для переключения MeasureFeed3 (Toggle MeasureFeed3)
function ToggleMeasureFeed3()
    ToggleFeed(3)
end

-- Функция для переключения MeasureFeed4 (Toggle MeasureFeed4)
function ToggleMeasureFeed4()
    ToggleFeed(4)
end

-- Функция для переключения MeasureFeed5 (Toggle MeasureFeed5)
function ToggleMeasureFeed5()
    ToggleFeed(5)
end
```

**Добавить после ToggleMeasureFeed5:**
```lua
-- Функция для переключения MeasureFeed6 (Toggle MeasureFeed6)
function ToggleMeasureFeed6()
    ToggleFeed(6)
end
```

**Зачем это нужно:**
Rainmeter требует, чтобы функции, вызываемые через `!CommandMeasure`, были определены в Lua-скрипте. Кнопка в OmniCal.ini вызывает `ToggleMeasureFeed6()`, которая передаёт управление основной функции `ToggleFeed(6)`.

---

#### ШАГ 6: OmniCal.ini — Создание кнопки Cal_6 в интерфейсе

**Файл:** `OmniCal.ini`

**Что делаем:**
Создать визуальную кнопку для управления календарём 6.

**Где добавить:**
Найдите секцию "КНОПКИ КАЛЕНДАРЕЙ" (строка ~92-160) и добавьте Cal_6 после Cal_5.

**Текущая структура кнопок (строки 95-157):**
```ini
;▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃▃
; КНОПКИ КАЛЕНДАРЕЙ слева вверху сетки (Calendar buttons at the top left of the grid)

    [Cal_1];──────────────────────────
    Meter=Shape
    X=(#LeftMarginTxt#+5)
    Y=10
    Shape=Ellipse 0,0,#EllipseRad1# | Fill Color #Cal_1_FillColor# | ...
    ...
    LeftMouseUpAction=[!CommandMeasure OmniCal_Skin_lua "ToggleMeasureFeed1()"]

    [Cal_2]
    Meter=Shape
    X=11r  ; ← 11 пикселей правее Cal_1
    Y=0r   ; ← на той же высоте
    ...

    [Cal_3]
    Meter=Shape
    X=11r  ; ← 11 пикселей правее Cal_2
    Y=0r
    ...

    [Cal_4];──────────────────────────
    Meter=Shape
    X=(#LeftMarginTxt#+5)  ; ← начало новой строки
    Y=13r  ; ← 13 пикселей ниже Cal_3
    ...

    [Cal_5];──────────────────────────
    Meter=Shape
    X=#Cal_5_OffsetX#r
    Y=#Cal_5_OffsetY#r
    Shape=Rectangle 0,0,#Rectangle5W#,#Rectangle5H#,2,2 | ...
    ...
    LeftMouseUpAction=[!CommandMeasure OmniCal_Skin_lua "ToggleMeasureFeed5()"]
```

**Расположение кнопок:**
```
Cal_1  Cal_2  Cal_3  ← Строка 1 (эллипсы)
Cal_4  Cal_5         ← Строка 2 (эллипс + прямоугольник)
```

**Добавить Cal_6 после Cal_5 (ВАРИАНТ 1 — в той же строке):**
```ini
    [Cal_6];──────────────────────────
    Meter=Shape
    X=3r
    Y=0r
    Shape=Rectangle 0,0,#Rectangle6W#,#Rectangle6H#,2,2 | Fill Color #Cal_6_FillColor# | StrokeWidth 1 | Stroke Color #Cal_6_StrokColor#
    DynamicVariables=1
    MouseOverAction=[!SetVariable Rectangle6H (#Rectangle6H#+#RectangleScale#)][!SetVariable Rectangle6W (#Rectangle6W#+#RectangleScale#)][!SetVariable Cal_6_OffsetX (#Cal_6_OffsetX#-1)][!SetVariable Cal_6_OffsetY (#Cal_6_OffsetY#-1)][!UpdateMeter Cal_6][!ShowMeter Cal_X_BG][!ShowMeter Cal_6_Text][!Redraw]
    MouseLeaveAction=[!SetVariable Rectangle6H (#Rectangle6H#-#RectangleScale#)][!SetVariable Rectangle6W (#Rectangle6W#-#RectangleScale#)][!SetVariable Cal_6_OffsetX (#Cal_6_OffsetX#+1)][!SetVariable Cal_6_OffsetY (#Cal_6_OffsetY#+1)][!UpdateMeter Cal_6][!HideMeter Cal_X_BG][!HideMeter Cal_6_Text][!Redraw]
    LeftMouseUpAction=[!CommandMeasure OmniCal_Skin_lua "ToggleMeasureFeed6()"]
```

**ВАРИАНТ 2 — на новой строке (если не помещается):**
```ini
    [Cal_6];──────────────────────────
    Meter=Shape
    X=(#LeftMarginTxt#+5)
    Y=13r
    Shape=Rectangle 0,0,#Rectangle6W#,#Rectangle6H#,2,2 | Fill Color #Cal_6_FillColor# | StrokeWidth 1 | Stroke Color #Cal_6_StrokColor#
    DynamicVariables=1
    MouseOverAction=[!SetVariable Rectangle6H (#Rectangle6H#+#RectangleScale#)][!SetVariable Rectangle6W (#Rectangle6W#+#RectangleScale#)][!UpdateMeter Cal_6][!ShowMeter Cal_X_BG][!ShowMeter Cal_6_Text][!Redraw]
    MouseLeaveAction=[!SetVariable Rectangle6H (#Rectangle6H#-#RectangleScale#)][!SetVariable Rectangle6W (#Rectangle6W#-#RectangleScale#)][!UpdateMeter Cal_6][!HideMeter Cal_X_BG][!HideMeter Cal_6_Text][!Redraw]
    LeftMouseUpAction=[!CommandMeasure OmniCal_Skin_lua "ToggleMeasureFeed6()"]
```

**Параметры кнопки:**
- `Meter=Shape` — графический элемент
- `X=3r` / `Y=0r` — относительная позиция (r = relative)
  - `X=3r` → 3 пикселя правее предыдущего элемента (Cal_5)
  - `Y=0r` → на той же высоте
- `Shape=Rectangle ...` — прямоугольник (как Cal_5, стиль агрегатора)
  - Размеры: `#Rectangle6W#` × `#Rectangle6H#`
  - Скругление углов: `2,2`
  - Заливка: `#Cal_6_FillColor#`
  - Обводка: `#Cal_6_StrokColor#` (толщина 1 пиксель)
- `DynamicVariables=1` — обновлять переменные при их изменении
- `MouseOverAction` — увеличить размер кнопки при наведении (эффект масштабирования)
- `MouseLeaveAction` — вернуть исходный размер
- `LeftMouseUpAction` — клик вызывает функцию `ToggleMeasureFeed6()`

**Дополнительные элементы (опционально):**
Если хотите добавить текстовую метку (название календаря):
```ini
    [Cal_6_Text]
    Meter=String
    X=([Cal_6:X])
    Y=([Cal_6:Y]+[Cal_6:H]+2)
    W=50
    H=12
    FontSize=8
    FontColor=255,255,255,200
    FontFace=#FontName#
    Text=#iCalendar6_name#
    StringAlign=Center
    AntiAlias=1
    DynamicVariables=1
    Hidden=1
```

**Новое расположение:**
```
Cal_1  Cal_2  Cal_3     ← Строка 1
Cal_4  Cal_5  Cal_6     ← Строка 2 (или Cal_6 на строке 3)
```

---

#### ШАГ 7: Variables.ini — Переменные параметров кнопки Cal_6

**Важно:** Этот скин работает с системой тем! **Цветовые переменные** добавляются ТОЛЬКО в файлы тем:
- `@Resources/Variables_Theme_Dark.ini`
- `@Resources/Variables_Theme_Light.ini`

В файл `@Resources/Variables.ini` добавляются ТОЛЬКО параметры кнопки (размеры, смещения).

---

##### ШАГ 7.1: Variables.ini — Параметры кнопки (размеры и позиция)

**Файл:** `@Resources/Variables.ini`

**Что делаем:**
Добавить переменные для параметров кнопки Cal_6 (размеры, смещения).

**Где добавить:**
Найдите секцию с параметрами кнопок календарей (примерно строка 460-480) и добавьте Cal_6.

**Найти существующие переменные Cal_5 (строки ~470-474):**
```ini
; Параметры кнопки Cal_5 (Cal_5 button parameters)
Rectangle5W=17
Rectangle5H=6
RectangleScale=2
Cal_5_OffsetX=7
Cal_5_OffsetY=-3
```

**Добавить после Cal_5:**
```ini
; Параметры кнопки Cal_6 (Cal_6 button parameters)
Rectangle6W=17
Rectangle6H=6
Cal_6_OffsetX=7
Cal_6_OffsetY=-3
```

**Описание переменных:**
- `Rectangle6W=17` — ширина прямоугольника кнопки (пиксели)
- `Rectangle6H=6` — высота прямоугольника кнопки (пиксели)
- `Cal_6_OffsetX=7` — смещение по горизонтали относительно предыдущей кнопки
- `Cal_6_OffsetY=-3` — смещение по вертикали (отрицательное = выше)

**Замечание:** Переменная `RectangleScale=2` общая для всех прямоугольных кнопок (Cal_5, Cal_6, ...), не нужно дублировать.

---

##### ШАГ 7.2: Variables_Theme_Dark.ini — Цвета для тёмной темы

**Файл:** `@Resources/Variables_Theme_Dark.ini`

**Что делаем:**
Добавить цветовые переменные для календаря 6 в тёмной теме.

**Где добавить:**
Найдите секцию с цветами календарей Cal_1-5 и добавьте Cal_6.

**Найти существующие переменные Cal_5:**
```ini
; Календарь 5 - Агрегатор (Calendar 5 - Aggregator)
Cal_5_Bg=138,43,226,255
Cal_5_FillColor=138,43,226,180
Cal_5_StrokColor=138,43,226
```

**Добавить после Cal_5:**
```ini
; Календарь 6 - Агрегатор (Calendar 6 - Aggregator)
Cal_6_Bg=255,128,0,255
Cal_6_FillColor=255,128,0,180
Cal_6_StrokColor=255,128,0
```

---

##### ШАГ 7.3: Variables_Theme_Light.ini — Цвета для светлой темы

**Файл:** `@Resources/Variables_Theme_Light.ini`

**Что делаем:**
Добавить цветовые переменные для календаря 6 в светлой теме.

**Где добавить:**
Найдите секцию с цветами календарей Cal_1-5 и добавьте Cal_6.

**Найти существующие переменные Cal_5:**
```ini
; Календарь 5 - Агрегатор (Calendar 5 - Aggregator)
Cal_5_Bg=138,43,226,255
Cal_5_FillColor=138,43,226,220
Cal_5_StrokColor=138,43,226
```

**Добавить после Cal_5:**
```ini
; Календарь 6 - Агрегатор (Calendar 6 - Aggregator)
Cal_6_Bg=255,128,0,255
Cal_6_FillColor=255,128,0,220
Cal_6_StrokColor=255,128,0
```

**Замечание:** В светлой теме `FillColor Alpha` можно увеличить до 220 для лучшей видимости кнопки на светлом фоне.

---

**Описание цветовых переменных:**

- `Cal_6_Bg` — основной цвет событий (RGBA: R, G, B, Alpha)
  - Пример: `255,128,0,255` = оранжевый, непрозрачный
  - Используется в OmniCal_Events_Renderer.lua для фона событий
  - **Формат:** `R,G,B,A` (Red, Green, Blue, Alpha)
  - **Диапазон:** каждое значение 0-255
  
- `Cal_6_FillColor` — цвет заливки кнопки (RGBA)
  - Тёмная тема: Alpha = 180
  - Светлая тема: Alpha = 220 (более непрозрачная для видимости)
  - Используется в кнопке [Cal_6]
  
- `Cal_6_StrokColor` — цвет обводки кнопки (RGB, без Alpha)
  - Обычно такой же цвет, как Cal_6_Bg, но без альфа-канала
  - Используется для контура кнопки
  - **Формат:** `R,G,B` (без Alpha)

**Выбор цвета:**
Рекомендуется использовать цвета, отличающиеся от Cal_1-5, чтобы события были различимы:
- Cal_1: Синий (0,120,215)
- Cal_2: Зелёный (16,124,16)
- Cal_3: Красный (229,20,0)
- Cal_4: Жёлтый (255,185,0)
- Cal_5: Фиолетовый (138,43,226)
- **Cal_6: Оранжевый (255,128,0)** ← рекомендуемый
- Альтернативы: 
  - Бирюзовый (0,183,195)
  - Розовый (232,17,35)
  - Коричневый (164,124,86)
  - Голубой (0,204,255)
  - Лаймовый (153,204,0)

**Онлайн инструменты для выбора цвета:**
- https://colorpicker.me/ — визуальный выбор цвета
- https://www.rapidtables.com/web/color/RGB_Color.html — таблица RGB цветов

---

#### ШАГ 8: Тестирование

1. Перезагрузите скин
2. Включите Cal_6
3. Проверьте `ActiveFeeds` → должно быть `...MeasureFeed6`
4. Проверьте Debug/CalendarEvents_dump.txt → события с `SourceFeed=6`
5. Проверьте UI → события отображаются с цветом `Cal_6_Bg`

---

### Итоговая схема для Calendar 6

```
Calendar 6 (Агрегатор)
├── iCalendar6_1 → MeasureFeed6_1 → События с SourceFeed=6
├── iCalendar6_2 → MeasureFeed6_2 → События с SourceFeed=6
└── iCalendar6_3 → MeasureFeed6_3 → События с SourceFeed=6

Все события → цвет Cal_6_Bg
```

---

### ✅ Полный чеклист для создания нового агрегатора

#### Файлы для изменения:

- [ ] **Variables_URL.ini** (ШАГ 1):
  - [ ] Добавить переменные `iCalendar6_1`, `iCalendar6_2`, `iCalendar6_3`
  - [ ] Добавить переменную `iCalendar6_name` с названием агрегатора
  - [ ] Убедиться, что переменная `iCalendar6` НЕ существует

- [ ] **OmniCal.ini** (ШАГ 2):
  - [ ] Создать меры `[MeasureFeed6_1]`, `[MeasureFeed6_2]`, `[MeasureFeed6_3]`
  - [ ] Каждая мера: `URL=#iCalendar6_N#`
  - [ ] Каждая мера: `FinishAction=[!CommandMeasure Reader_lua Refresh(1)]`
  - [ ] Удалить старую меру `[MeasureFeed6]` (если существовала)

- [ ] **Reader.lua — Initialize()** (ШАГ 3):
  - [ ] Добавить блок `elseif MeasureName == 'MeasureFeed6' then`
  - [ ] Цикл `for subIdx = 1, 3 do` (количество подкалендарей)
  - [ ] Поиск мер `MeasureFeed6_1`, `MeasureFeed6_2`, `MeasureFeed6_3`
  - [ ] Проверка URL `iCalendar6_N` на непустоту

- [ ] **Reader.lua — Update()** (ШАГ 3):
  - [ ] Дублировать логику из Initialize() для агрегатора 6
  - [ ] Убедиться, что `lastMeasureName` корректно обновляется

- [ ] **OmniCal_Skin.lua — validFeeds** (ШАГ 4.1):
  - [ ] Добавить блок `elseif n == 6 then`
  - [ ] Цикл проверки `iCalendar6_1` - `iCalendar6_3`
  - [ ] Если хотя бы один URL непустой → `table.insert(validFeeds, 6)`

- [ ] **OmniCal_Skin.lua — ToggleFeed URL management** (ШАГ 4.2):
  - [ ] Добавить блок `elseif i == 6 then`
  - [ ] Цикл `for subIdx = 1, 3 do`
  - [ ] Управление URL для `MeasureFeed6_1` - `MeasureFeed6_3`
  - [ ] Команды `!SetOption`, `!EnableMeasure`, `!DisableMeasure`

- [ ] **OmniCal_Skin.lua — ToggleMeasureFeed6()** (ШАГ 5):
  - [ ] Создать функцию `function ToggleMeasureFeed6() ToggleFeed(6) end`
  - [ ] Разместить после `ToggleMeasureFeed5()`

- [ ] **OmniCal.ini — Кнопка [Cal_6]** (ШАГ 6):
  - [ ] Создать метр `[Cal_6]`
  - [ ] Задать позицию `X=3r` / `Y=0r` (или `X=(#LeftMarginTxt#+5)` / `Y=13r`)
  - [ ] Shape: `Rectangle 0,0,#Rectangle6W#,#Rectangle6H#,2,2`
  - [ ] Цвета: `#Cal_6_FillColor#`, `#Cal_6_StrokColor#`
  - [ ] Action: `LeftMouseUpAction=[!CommandMeasure OmniCal_Skin_lua "ToggleMeasureFeed6()"]`
  - [ ] MouseOverAction / MouseLeaveAction для эффекта масштабирования

- [ ] **Variables.ini — Параметры кнопки Cal_6** (ШАГ 7.1):
  - [ ] `Rectangle6W=17` (ширина кнопки)
  - [ ] `Rectangle6H=6` (высота кнопки)
  - [ ] `Cal_6_OffsetX=7` (смещение по X)
  - [ ] `Cal_6_OffsetY=-3` (смещение по Y)

- [ ] **Variables_Theme_Dark.ini — Цвета Cal_6** (ШАГ 7.2):
  - [ ] `Cal_6_Bg=255,128,0,255` (основной цвет событий)
  - [ ] `Cal_6_FillColor=255,128,0,180` (цвет заливки кнопки, Alpha=180)
  - [ ] `Cal_6_StrokColor=255,128,0` (цвет обводки)

- [ ] **Variables_Theme_Light.ini — Цвета Cal_6** (ШАГ 7.3):
  - [ ] `Cal_6_Bg=255,128,0,255` (основной цвет событий)
  - [ ] `Cal_6_FillColor=255,128,0,220` (цвет заливки кнопки, Alpha=220 для видимости)
  - [ ] `Cal_6_StrokColor=255,128,0` (цвет обводки)

#### Тестирование (ШАГ 8):

- [ ] Перезагрузить скин (Refresh)
- [ ] Открыть Debug/LOG.txt → включить DebugMode=1
- [ ] Клик на Cal_6 → проверить логи:
  - [ ] `===TF: Валидные календари: ...6`
  - [ ] `~~~~~~~~TF.ON: MeasureFeed6_1 iCalendar6_1 https://...`
  - [ ] `~~~~~~~~TF.ON: MeasureFeed6_2 iCalendar6_2 https://...`
  - [ ] `[MeasureFeed6_1]: Fetching: https://...`
- [ ] Проверить ActiveFeeds в Variables_URL.ini → содержит `MeasureFeed6`
- [ ] Открыть Debug/CalendarEvents_dump.txt → события имеют `SourceFeed=6`
- [ ] Проверить UI:
  - [ ] Кнопка Cal_6 видна и кликабельна
  - [ ] События отображаются с цветом `Cal_6_Bg` (оранжевый)
  - [ ] MouseOver на кнопке → увеличение размера
- [ ] Клик на Cal_6 ещё раз (отключение):
  - [ ] `~~~~~~~~TF.OFF: MeasureFeed6_1 iCalendar6_1 ''`
  - [ ] События исчезают с сетки
  - [ ] Нет запросов к URL в логах

#### Типичные ошибки:

- ❌ Забыли удалить переменную `iCalendar6` → конфликт с подкалендарями
- ❌ Забыли добавить `ToggleMeasureFeed6()` → кнопка не работает
- ❌ Не обновили `Update()` в Reader.lua → агрегатор не работает при изменении ActiveFeeds
- ❌ Забыли добавить цвета в Variables.ini → события отображаются с цветом по умолчанию
- ❌ Не добавили блок `elseif n == 6` в validFeeds → календарь считается невалидным
- ❌ Забыли переменные Rectangle6W/H → кнопка не отображается или имеет нулевой размер

---

## 🔧 РАСШИРЕНИЕ ТЕКУЩЕГО АГРЕГАТОРА

### Задача: Добавить 6-й и 7-й подкалендарь в Calendar 5

#### ШАГ 1: Variables_URL.ini

```ini
iCalendar5_1=...
iCalendar5_2=...
iCalendar5_3=...
iCalendar5_4=...
iCalendar5_5=...
iCalendar5_6=https://...  ← НОВЫЙ
iCalendar5_7=https://...  ← НОВЫЙ
```

#### ШАГ 2: OmniCal.ini

Добавьте 2 новые меры:

```ini
[MeasureFeed5_6]
Measure=WebParser
URL=#iCalendar5_6#
...

[MeasureFeed5_7]
Measure=WebParser
URL=#iCalendar5_7#
...
```

#### ШАГ 3: Reader.lua — Initialize() и Update()

Измените цикл:

```lua
if MeasureName == 'MeasureFeed5' then
    for subIdx = 1, 7 do  -- ← БЫЛО: 1, 5
        local subMeasureName = 'MeasureFeed5_' .. subIdx
        ...
    end
```

#### ШАГ 4: OmniCal_Skin.lua — ToggleFeed()

##### validFeeds:

```lua
if n == 5 then
    for subIdx = 1, 7 do  -- ← БЫЛО: 1, 5
        ...
    end
```

##### Управление URL:

```lua
if i == 5 then
    for subIdx = 1, 7 do  -- ← БЫЛО: 1, 5
        ...
    end
```

#### ШАГ 5: Тестирование

Перезагрузите скин → проверьте, что события из 6-го и 7-го подкалендарей отображаются.

---

## 🐛 ОТЛАДКА И TROUBLESHOOTING

### Проблема: Агрегатор не включается

**Симптомы:**
- Клик на Cal_5 → кнопка не меняет цвет
- ActiveFeeds не содержит MeasureFeed5

**Решение:**
1. Проверьте `validFeeds` в OmniCal_Skin.lua:
   ```lua
   if tonumber(SKIN:GetVariable('DebugMode')) == 1 then
       print("===TF: Валидные календари: " .. table.concat(validFeeds, ','))
   end
   ```
2. Убедитесь, что хотя бы один `iCalendar5_N` не пустой
3. Проверьте, что переменная `iCalendar5` **не существует** (конфликт)

**История бага:**
- **БАГ #1 (исправлен 12.11.2025)**: Функция ToggleFeed() проверяла `iCalendar5`, которая была удалена при переходе на агрегатор. Календарь 5 не попадал в validFeeds. Исправлено добавлением проверки iCalendar5_1-5 в цикле validFeeds.

---

### Проблема: События не отображаются

**Симптомы:**
- Cal_5 включён
- ActiveFeeds содержит MeasureFeed5
- Но событий нет на экране

**Решение:**
1. Проверьте URL в Variables_URL.ini (корректность ссылок)
2. Откройте Debug/LOG.txt → ищите ошибки парсинга
3. Проверьте Feeds[] в Reader.lua:
   ```lua
   print("Reader.lua-Initialize(): Feeds[] содержит:")
   for i, feed in ipairs(Feeds) do
       print("  " .. i .. ": " .. feed.MeasureName)
   end
   ```
4. Убедитесь, что меры `[MeasureFeed5_1-5]` существуют в OmniCal.ini

**История бага:**
- **БАГ #3 (исправлен 12.11.2025)**: Цикл в ToggleFeed() проверял `url = SKIN:GetVariable('iCalendar' .. i)` ДО проверки `if i == 5`. Для i=5 получал nil → break → блок агрегатора не выполнялся. События не загружались при включении Cal_5. Исправлено перемещением `if i == 5` ПЕРЕД чтением переменной.

---

### Проблема: Агрегатор парсит данные при отключенном состоянии

**Симптомы:**
- ActiveFeeds = MeasureFeed1|MeasureFeed2 (без MeasureFeed5)
- В логах видны запросы от MeasureFeed5_1, MeasureFeed5_2, ...
- Лишний трафик при загрузке скина

**Решение:**
1. Проверьте, что DisableInactiveFeedsOnLoad() вызывается при загрузке скина
2. Включите DebugMode=1 и проверьте логи:
   ```
   ===TF: OFF MeasureFeed5_1 | iCalendar5_1 = '' (aggregator disabled)
   ===TF: OFF MeasureFeed5_2 | iCalendar5_2 = '' (aggregator disabled)
   ```
3. Убедитесь, что используется двойная защита:
   - Пустой URL: `SKIN:Bang('!SetOption', measureSubName, 'URL', '')`
   - Отключение меры: `SKIN:Bang('!DisableMeasure', measureSubName)`

**История бага:**
- **БАГ #2 (исправлен 12.11.2025)**: Функция DisableInactiveFeedsOnLoad() не содержала специальной обработки для агрегатора. При загрузке скина подмеры MeasureFeed5_1-5 парсили данные до вызова функции отключения. Исправлено добавлением цикла для i=5 с !DisableMeasure + пустым URL.

---

### Проблема: В логах отсутствуют сообщения о включении/выключении подмер

**Симптомы:**
- Клик на Cal_5 ON → нет логов "~~~~~~~~TF.ON: MeasureFeed5_1..."
- Клик на Cal_5 OFF → нет логов "~~~~~~~~TF.OFF: MeasureFeed5_1..."
- События не появляются/исчезают

**Решение:**
1. Проверьте порядок операций в цикле ToggleFeed():
   ```lua
   for i = 1, 10 do
       if i == 5 then
           -- Этот блок должен выполняться ПЕРВЫМ для i=5
           for subIdx = 1, 5 do
               -- Обработка агрегатора
           end
       else
           local url = SKIN:GetVariable('iCalendar' .. i)
           if url == nil then break end  -- Этот break не должен влиять на i=5
       end
   end
   ```
2. Убедитесь, что переменная `iCalendar5` НЕ существует (иначе цикл не сломается, но логика будет неверной)

**История бага:**
- **БАГ #3 (исправлен 12.11.2025)**: Критический баг! Цикл в ToggleFeed() выполнял `local url = SKIN:GetVariable('iCalendar' .. i)` ДО проверки `if i == 5`. Для i=5 переменная iCalendar5 не существует → url == nil → break → цикл завершается, блок агрегатора не выполняется. Исправлено изменением порядка: сначала `if i == 5`, потом чтение переменной для остальных.

---

### Проблема: Неправильная сортировка

**Симптомы:**
- События из 5_3 отображаются левее событий из 5_1 при одинаковом времени

**Решение:**
1. Проверьте функцию `compareTime()` в Reader.lua
2. Убедитесь, что `Item.MeasureName` сохраняется:
   ```lua
   Item.MeasureName = Feeds[f].MeasureName
   ```
3. Проверьте regex в compareTime:
   ```lua
   local aSubIdx = tonumber((a.MeasureName or ''):match('_(%d+)$')) or 0
   ```

---

### Проблема: Все подкалендари имеют разные цвета

**Симптомы:**
- События из 5_1 — синие
- События из 5_2 — красные
- Должны быть одного цвета (Cal_5_Bg)

**Решение:**
1. Проверьте regex для SourceFeed в Reader.lua:
   ```lua
   local sourceNum = tonumber(Feeds[f].MeasureName:match('MeasureFeed(%d+)')) or f
   ```
2. Убедитесь, что regex берёт **первое** число:
   - `MeasureFeed5_1` → `5` ✅
   - НЕ `MeasureFeed5_1` → `1` ❌
3. Проверьте Debug/CalendarEvents_dump.txt:
   ```
   Item1SourceFeed=5  ← должно быть 5
   Item2SourceFeed=5
   ```

---

### Известные баги и их исправления (Bug History)

#### БАГ #1: validFeeds не определяет агрегатор как валидный
- **Дата обнаружения:** 12 ноября 2025
- **Дата исправления:** 12 ноября 2025
- **Затронутый файл:** OmniCal_Skin.lua (функция ToggleFeed, цикл validFeeds)
- **Симптом:** После отключения Cal_5 невозможно снова включить календарь
- **Причина:** Проверка `SKIN:GetVariable('iCalendar5')` возвращала nil (переменная заменена на iCalendar5_1-5)
- **Исправление:** Добавлена специальная проверка для n=5 с поиском хотя бы одного непустого iCalendar5_N
- **Статус:** ✅ Исправлено

#### БАГ #2: DisableInactiveFeedsOnLoad игнорирует агрегатор
- **Дата обнаружения:** 12 ноября 2025
- **Дата исправления:** 12 ноября 2025
- **Затронутый файл:** OmniCal_Skin.lua (функция DisableInactiveFeedsOnLoad)
- **Симптом:** При загрузке скина без MeasureFeed5 в ActiveFeeds подмеры 5_1-5 парсят данные → лишний трафик
- **Причина:** Функция содержала логику только для обычных календарей (MeasureFeed1-4), агрегатор не обрабатывался
- **Исправление:** Добавлен блок `if i == 5` с циклом для subIdx=1,5 и командами !SetOption URL '' + !DisableMeasure
- **Оптимизация:** Двойная защита (пустой URL + отключение меры)
- **Статус:** ✅ Исправлено

#### БАГ #3: Цикл ToggleFeed() завершается до обработки агрегатора
- **Дата обнаружения:** 12 ноября 2025
- **Дата исправления:** 12 ноября 2025
- **Затронутый файл:** OmniCal_Skin.lua (функция ToggleFeed, цикл управления URL)
- **Симптом:** В логах отсутствуют сообщения "~~~~~~~~TF.ON/OFF: MeasureFeed5_1...", события не появляются при включении Cal_5
- **Причина:** Цикл выполнял `local url = SKIN:GetVariable('iCalendar' .. i)` ДО проверки `if i == 5`. Для i=5 переменная iCalendar5 не существует → url == nil → break → блок агрегатора не выполняется
- **Исправление:** Перемещена проверка `if i == 5` ПЕРЕД попыткой чтения переменной iCalendar. Теперь для i=5 сразу выполняется агрегаторная логика без проверки несуществующей переменной
- **Статус:** ✅ Исправлено (критический баг)
- **Важность:** Без этого исправления агрегатор полностью нефункционален

---

### Включение отладки (Enable debugging)

В Variables.ini:

```ini
DebugMode=1
```

Перезагрузите скин → откройте Rainmeter → View → Log → уровень Debug.

---

## ❓ FAQ

### Q1: Можно ли создать агрегатор из агрегаторов?

**A:** Технически — да, но не рекомендуется. Это усложнит логику и затруднит отладку. Лучше увеличить количество подкалендарей в одном агрегаторе.

---

### Q2: Сколько максимум подкалендарей в агрегаторе?

**A:** Ограничений нет (кроме производительности). Текущий Calendar 5 = 5 подкалендарей, но можно сделать 10, 20, 100. Просто измените циклы:

```lua
for subIdx = 1, 100 do  -- ← вместо 1, 5
```

---

### Q3: Можно ли сделать все календари агрегаторами?

**A:** Да! Можно сделать Cal_1-5 агрегаторами:
- Cal_1 = агрегатор (3 подкалендаря)
- Cal_2 = агрегатор (2 подкалендаря)
- Cal_3 = обычный календарь
- Cal_4 = агрегатор (5 подкалендарей)
- Cal_5 = агрегатор (5 подкалендарей)

Нужно добавить обработку для каждого в Reader.lua и OmniCal_Skin.lua.

---

### Q4: Как различить события из разных подкалендарей визуально?

**A:** Сейчас все события агрегатора имеют один цвет. Варианты:

1. **Иконки**: Добавить поле `Item.SubIndex` и отображать маленькую иконку (1, 2, 3, ...)
2. **Тултипы**: В tooltip выводить `MeasureName` (MeasureFeed5_1, MeasureFeed5_2, ...)

---

### Q5: Что если хочу динамическое количество подкалендарей?

**A:** Сейчас количество фиксировано (5 для Cal_5). Для динамики нужно:

1. В Variables_URL.ini добавить переменную:
   ```ini
   iCalendar5_subcount=3
   ```

2. В Reader.lua:
   ```lua
   local subcount = tonumber(SKIN:GetVariable('iCalendar5_subcount')) or 5
   for subIdx = 1, subcount do
       ...
   end
   ```

3. В OmniCal.ini создать **больше мер**, чем нужно (например, 10), лишние будут пропущены.

---

### Q6: Как переименовать подкалендарь в UI?

**A:** Сейчас все подкалендари скрыты под общим именем `iCalendar5_name`. Для раздельных имён:

1. Добавьте переменные:
   ```ini
   iCalendar5_1_name=Личный
   iCalendar5_2_name=Консультации
   ```

2. В Reader.lua сохраняйте:
   ```lua
   Item.SubcalendarName = SKIN:GetVariable('iCalendar5_' .. subIdx .. '_name')
   ```

3. В tooltip отображайте `Item.SubcalendarName`

---


## ✅ ИТОГИ (SUMMARY)

### Что нужно для создания агрегатора (Checklist)

- [ ] **Variables_URL.ini**: Переменные `iCalendarN_1` до `iCalendarN_M`
- [ ] **OmniCal.ini**: Меры `[MeasureFeedN_1]` до `[MeasureFeedN_M]`
- [ ] **Reader.lua Initialize()**: Автопоиск подмер при `MeasureFeedN`
- [ ] **Reader.lua Update()**: Дублирование логики Initialize
- [ ] **Reader.lua compareTime()**: Сортировка по subIdx
- [ ] **OmniCal_Skin.lua validFeeds**: Проверка подкалендарей
- [ ] **OmniCal_Skin.lua URL control**: Управление подмерами
- [ ] **OmniCal.ini кнопка**: `[Cal_N]` с `LeftMouseUpAction`
- [ ] **Variables.ini**: Цвета `Cal_N_Bg`, `Cal_N_FillColor`, `Cal_N_StrokColor`
- [ ] **Тестирование**: Включение, отключение, агрегация, сортировка

---

**Автор документации:** AI Assistant (GitHub Copilot)  
**Дата:** 12 ноября 2025 г.  
**Версия OmniCal:** 3.5+  
**Последнее обновление:** 12 ноября 2025 г. (добавлена история багов #1, #2, #3)

**Лицензия:** CC BY-NC-SA 3.0  
**Вопросы и предложения:** [GitHub Issues](https://github.com/Dokcc/Rainmeter-OmniCalendar/issues)

---

## 📋 ИСТОРИЯ ИЗМЕНЕНИЙ ДОКУМЕНТА

### Версия 1.0 (12 ноября 2025)
- Создан первоначальный документ
- Описана архитектура агрегатора Calendar 5
- Добавлены инструкции по созданию нового агрегатора
- Описан поток данных

### Версия 1.1 (12 ноября 2025)
- Добавлена секция "Известные баги и их исправления"
- Документированы БАГ #1 (validFeeds), БАГ #2 (DisableInactiveFeedsOnLoad), БАГ #3 (ToggleFeed loop)
- Обновлены troubleshooting секции с ссылками на историю багов
- Добавлены решения для проблем с включением/выключением агрегатора
