# Маркировка повторяющегося события

♻⥀ ⥁ ↺↻

`Reader.lua 424:`

```lua
            local hasRRule = selected.Raw and selected.Raw:match("RRULE")
            if (hasRRule or (#UIDGrouped[uid] > 1)) and not selected.Title:find("♻") then
                selected.Title = (selected.Title or "") .. " ♻"
            end
```
