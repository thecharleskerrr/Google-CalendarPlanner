1) Variables.ini
	— добавить:
		• Cal_5_FillColor=...
		• Cal_5_StrokColor=...
        
2)  Variables_Theme_(Theme).ini
;—— СОБЫТИЕ (Event) iCalendar N ———————————
    ; Оформление события для N-го календаря (Event styling for the Nst calendar):
    ; Цвет фона для событий (Background color for events):
    Cal_N_Bg=...
    ; "Выцведший" вариант цвета фона для прошедших событий (Faded background color variant for past events):
    Cal_N_Bg_Faded=...

3) Variables_URL.ini
	— добавить:
		• iCalendarN_name=...
		• iCalendarN=...

4)  OmniCal.ini
	— добавить:
		• [Cal_N]
			Поменять:
			• #EllipseRadN#
			• #Cal_N_FillColor#
			• #Cal_N_StrokColor#
			• ToggleMeasureFeedN()
	— добавить:
		• [MeasureFeedN]
			Поменять:
			• #iCalendarN#
	— добавить:
		• [Cal_N_Text]
			Поменять:
			• #iCalendarN_name#

5)  OmniCal_Skin.lua
	— добавить:
		    function ToggleMeasureFeedN()
			ToggleFeed(N)
		    end
	— function ToggleFeed(feedIndex)
		добавить: calendarCount = N
```
        if file then
            local content = file:read('*all')
            file:close()
            for match in content:gmatch('iCalendar%d+=') do
                calendarCount = calendarCount + 1
            end
        else
            calendarCount = N -- Fallback to N if file not found
        end
```
